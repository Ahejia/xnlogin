server:
  port: 9990
#设置实体所在的包，然后在mapper.xml里面不用写实体的全名，直接写实体的以小写字母开头的简单类名即可,例如：com.tracker.entity.User,可以直接写user
mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.example.xnlogin.mess
  configuration:
    mapUnderscoreToCamelCase: true

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/test?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowMultiQueries=true
    username: xiayutian
    password: 123456
    driverClassName: com.mysql.jdbc.Driver
